source "https://rubygems.org"

# Use the latest Jekyll version compatible with GitHub Pages
# Remove the explicit jekyll gem if you're using github-pages
# gem "jekyll", "~> 4.3.4"

# Update Minima to the latest version
gem "minima", "~> 2.5.1"

# If you're using GitHub Pages, include the github-pages gem
gem "github-pages", group: :jekyll_plugins

group :jekyll_plugins do
  gem "jekyll-feed", "~> 0.12"
end

# Rest of your Gemfile remains the same
